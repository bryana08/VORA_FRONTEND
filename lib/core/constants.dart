import 'package:flutter/material.dart';

// Design tokens extraits des maquettes VORA
class AppColors {
  static const green = Color(0xFF3DAE4E);
  static const greenDark = Color(0xFF0E5B33);
  static const greenLight = Color(0xFFEAF6EC);

  static const dark = Color(0xFF12181A);
  static const navy = Color(0xFF111827);

  static const bg = Color(0xFFF6F7F5);
  static const card = Color(0xFFFFFFFF);
  static const border = Color(0xFFE4E5E2);

  static const muted = Color(0xFF7C868A);
  static const yellow = Color(0xFFF0B429);
  static const danger = Color(0xFFE53935);
}

class AppSpacing {
  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 16.0;
  static const lg = 24.0;
  static const xl = 32.0;
}

class AppRadius {
  static const sm = 8.0;
  static const md = 14.0;
  static const lg = 20.0;
  static const pill = 999.0;
}
